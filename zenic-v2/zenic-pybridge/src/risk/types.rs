//! Shared types and utilities for the risk prediction module.
//!
//! This module re-exports the common imports used across the risk
//! sub-modules (calculator and assessor).

// No custom types are needed — all functions operate on primitive
// PyO3 types and standard collections. This file exists as the
// canonical place for shared type definitions should they be
//! introduced in the future.
